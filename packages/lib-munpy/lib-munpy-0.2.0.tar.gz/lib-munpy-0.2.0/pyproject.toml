[project]
name = "lib-munpy"
version = "0.2.0"
authors = [
  { name="Libelium", email="n.gomariz@libelium.com" },
]
description = "Package to process MUNICH input/output data"
requires-python = ">=3.8"
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}
