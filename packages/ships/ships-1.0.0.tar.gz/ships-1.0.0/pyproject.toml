
#
#	publishing
#
#		(rm -rf dist && python3 -m build --sdist && twine upload dist/*)
#		(git add --all && git commit -m "com" && git push --set-upstream git@gitlab.com:reptilian_climates/BOTANY.git CLIMATE)
#

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ships"
version = "1.0.0"
description = "Various calculator tools"

license = { text = "GPL 3.0" }

readme = "ships.txt"
dependencies = [
	'homeostasis',
	
	'psutil',
	'pyinotify'
]

[tool.setuptools.packages.find]
where = [ "fields/gardens" ]

#
#	https://setuptools.pypa.io/en/latest/userguide/datafiles.html
#
[tool.setuptools.package-data]
"*" = [ "*.HTML" ]