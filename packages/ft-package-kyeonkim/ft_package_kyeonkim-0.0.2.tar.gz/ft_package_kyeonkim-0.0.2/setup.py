from setuptools import setup, find_packages

setup(
    name='ft_package_kyeonkim',
    version='0.0.2',
    description='A sample test package',
    author='kyeonkim',
    author_email='kyeonkim@student.42seoul.kr',
    # url='',
    # install_requires=[],
    # packages=find_packages(exclude=[]),
    keywords=['kyeonkim', 'ft_package'],
    # python_requires='>=3.6',
    # package_data={},
    # zip_safe=False,
    # classifiers=[],
)
