from setuptools import setup

setup(name='namataa_distributions_probability',
      version='0.1',
      description='Gaussian distributions',
      packages=['namataa_distributions_probability'],
      zip_safe=False)
