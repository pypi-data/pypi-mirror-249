[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mtok"
version = "2.0.0"
authors = [{ name = "Mahdi Mir", email = "imahdimir@gmail.com" }]
description = "Gets the token from the private tokens repository."
readme = "README.md"
license = { file = "LICENSE" }
dependencies = [
    "requests",
]
