[tool.poetry]
name = "http-jsonrpc"
version = "0.1.5"
description = ""
authors = ["Kamenlom <ivashchenko.roman111@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.27.1"

[tool.poetry.dev-dependencies]
pytest = "^7.4"
vcrpy = "^5.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
