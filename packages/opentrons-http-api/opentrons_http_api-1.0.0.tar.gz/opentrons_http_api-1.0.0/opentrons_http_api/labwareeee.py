from __future__ import annotations

from typing import Dict


class LabwareOffset:
    uri: str

    def to_dict(self) -> Dict:
        return {
            'data': ...,
        }

    # consider upstream http api too\
    # consider upstream http api too\
    # consider upstream http api too\
    # consider upstream http api too\
    # consider upstream http api too
