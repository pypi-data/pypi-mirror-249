from setuptools import setup, find_packages

setup(
    name='easy_pastebin',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        'requests',
    ],
    license='MIT'
)
