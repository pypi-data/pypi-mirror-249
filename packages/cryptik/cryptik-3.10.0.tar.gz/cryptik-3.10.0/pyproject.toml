[internal]
created = 2013-12-31
modified = 2024-01-06

[tool.poetry]
name = "cryptik"
version = "3.10.0"
description = "cryptocurrency prices on the command line"
authors = ["David Rader <sa@adercon.com>"]
maintainers = ["David Rader <sa@adercon.com>"]
license = "GPL-3.0-only"
readme = "README.md"
homepage = "https://gitlab.com/drad/cryptik"
repository = "https://gitlab.com/drad/cryptik"
keywords = ["cli", "crypto", "currency", "bitcoin", "ticker"]

[tool.poetry.scripts]
cryptik = "cryptik.main:app"

[tool.poetry.dependencies]
python = "^3.8"
arrow = "1.3.0"
click = "8.1.7"
tomli = "^2.0.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
