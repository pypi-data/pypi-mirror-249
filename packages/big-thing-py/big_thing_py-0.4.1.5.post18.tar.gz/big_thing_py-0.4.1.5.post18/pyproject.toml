
[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "big-thing-py"
version = "0.4.1.5-18"
description = "MySSIX Thing SDK"
authors = ["caplab <caplab94@gmail.com>"]
readme = "README.md"
packages = [{include = "big_thing_py"}]

[tool.poetry.dependencies]
python = "^3.7"

termcolor = "^2.3.0"
getmac = "^0.9.4"
paho-mqtt = "^1.6.1"
requests = "^2.31.0"
zeroconf = "^0.69.0"
websockets = "^11.0.3"
func-timeout = "^4.3.5"
typing-extensions = "^4.0"
dataclasses = "^0.6"
pytest = "^7.4.3"
pytest-timeout = "^2.2.0"
pytest-asyncio = "^0.23.2"


[tool.poetry.group.dev.dependencies]
tox = "^4.6.3"
