from setuptools import setup, find_packages

import easier_batch
setup(
    name='easier-batch',
    version='0.6',
    packages=[],
    install_requires=[
        # List your dependencies here
    ],
)
