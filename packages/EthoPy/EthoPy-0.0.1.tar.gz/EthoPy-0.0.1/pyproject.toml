[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "EthoPy"
version = "0.0.1"
readme = "README.md"
authors = [{ name = "Emmanouil Froudarakis", email = "frouman@imbb.forth.gr" }]
dependencies = ['datajoint', 'pygame', 'panda3D', 'numpy', 'scipy']
requires-python = ">=3.7"