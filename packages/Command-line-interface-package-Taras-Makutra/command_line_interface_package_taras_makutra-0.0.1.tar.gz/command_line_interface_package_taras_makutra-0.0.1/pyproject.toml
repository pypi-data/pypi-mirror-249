[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/foo"]

[project]
name = "Command_line_interface_package_Taras_Makutra"
version = "0.0.1"
authors = [
  { name="Taras Makutra", email="tarik9598@gmail.com" },
]
description = "a program that counts unique characters"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://git.foxminded.ua/foxstudent106344/task-4-cli"
